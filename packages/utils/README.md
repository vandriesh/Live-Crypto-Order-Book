# @neet/utils

Purpose: hold small shared helpers that are generic enough to reuse across features without pulling in domain-specific UI or transport concerns.

Current status:
- contains formatting helpers for market labels and numeric values
- used by `@neet/app-shell` and `@neet/order-book`
