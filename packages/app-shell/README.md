# @neet/app-shell

Thin application shell package.

Current responsibility:
- provide the Binance-like outer layout
- keep static shell surfaces out of the order book package
- accept plain `children` for now instead of a slot parser

The order book feature remains the focus of the challenge, so this package stays intentionally shallow.
